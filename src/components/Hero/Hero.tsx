const Hero = () => {
  return (
    <section id="hero">
      <div className="container">
        <h1>Tidjee's Todo List</h1>
        <p>
          This is a simple Todo List app built with React and TypeScript using
          Vite.
        </p>
      </div>
    </section>
  );
};

export default Hero;
